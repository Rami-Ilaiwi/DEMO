import { createStyles } from "@material-ui/core/styles";

export const styles = () =>
  createStyles({
    root: { float: "right", marginRight: "2%" },
    deleteIcon: {
      color: "rgba(0, 0, 0, 0.54)",
      cursor: "pointer",
      fontSize: "1.3rem"
    }
  });
