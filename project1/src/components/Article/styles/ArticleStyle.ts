import { createStyles } from "@material-ui/core/styles";

export const styles = () =>
  createStyles({
    root: {
      fontFamily: "'Roboto', 'Helvetica', 'Arial', sans-serif"
    },
    articleBody: {
      textDecoration: "none",
      color: "inherit"
    },
    title: {
      fontSize: "2.5rem",
      color: "inherit"
    }
  });
